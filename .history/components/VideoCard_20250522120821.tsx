import React from 'react'

const VideoCard = ({
    id,
    title,
    thumbnail,
    createdAt,
    userImg,
    views,
    visibility,
    duration
}: VideoCardProps) => {
  return (
    <div>VideoCard</div>
  )
}

export default VideoCard