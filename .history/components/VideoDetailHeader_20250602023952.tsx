import React from 'react'

const VideoDetailHeader = ({title, createdAt, userImg, username, videoId, ownerId, visibility, thumbnailUrl}: VideoDetailHeaderProps) => {
  return (
    <div>VideoDetailHeader</div>
  )
}

export default VideoDetailHeader